spring:
  application:
    # 应用名称
    name: @artifactId@
  profiles:
    # 环境配置
    active: @profile.name@
  cloud:
    nacos:
      discovery:
        # 解决获取IP为内网，一般微服务采用内网安全性、可用性高
        # ip: 127.0.0.1
        # 服务注册地址
        server-addr: @discovery.server-addr@
        # 命名空间
        namespace: @discovery.namespace@
        # 配置组
        group: @discovery.group@
      config:
        # 配置中心地址
        server-addr: @config.server-addr@
        # 命名空间
        namespace: @config.namespace@
        # 配置组
        group: @config.group@
        # 配置文件格式
        file-extension: @config.file-extension@
    # 暂时修复因sentinel导致的无法启动(maven 配置bootstrap,sentinel开启feign注册失败，application配置，config.import需要全量引入配置文件 )
    openfeign:
      lazy-attributes-resolution: true
  config:
    import:
      - optional:nacos:mesh-environment-${spring.profiles.active}.${spring.cloud.nacos.config.file-extension}
      - optional:nacos:${spring.application.name}-${spring.profiles.active}.${spring.cloud.nacos.config.file-extension}