<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.platform.mesh.app.biz.modules.app.modulesettransauto.mapper.AppModuleSetTransAutoMapper">


    <select id="selectTransAuthPage"
            resultType="com.platform.mesh.app.biz.modules.app.modulesettransauto.domain.bo.AppModuleSetTransAutoBO">
        SELECT
            a.*,
            t.del_from
        FROM
        app_module_set_trans_auto a
        LEFT JOIN app_module_set_trans t ON a.trans_id = t.id
        LEFT JOIN app_module_base m ON a.module_from_id = m.id
        <where>
            <if test=" pageDTO.moduleIds!=null and pageDTO.moduleIds.size() > 0 ">
                AND a.module_from_id IN
                <foreach collection="pageDTO.moduleIds" item="moduleId" open="(" close=")" separator=",">
                    #{moduleId}
                </foreach>
            </if>
            <if test=" pageDTO.moduleSchemas!=null and pageDTO.moduleSchemas.size() > 0 ">
                AND m.module_schema IN
                <foreach collection="pageDTO.moduleSchemas" item="moduleSchema" open="(" close=")" separator=",">
                    #{moduleSchema}
                </foreach>
            </if>
        </where>
    </select>
</mapper>