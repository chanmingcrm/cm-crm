<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.platform.mesh.ai.biz.modules.ai.mcp.tool.mapper.AiMcpToolMapper">

    <select id="selectEnabledTools"
            resultType="com.platform.mesh.ai.biz.modules.ai.mcp.tool.domain.po.AiMcpTool">
        SELECT id, mcp_module, tool_name, description, input_schema, execution_config,
               read_only, return_direct, status,
               create_time, update_time
        FROM ai_mcp_tool
        WHERE status = 1
        ORDER BY mcp_module, id
    </select>

</mapper>
